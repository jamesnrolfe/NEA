import ReactDOM from "react-dom";

const App = () => {
	// default/base function component
	return (
		<>
			<div className="placeholder">App component</div>
		</>
	);
};

ReactDOM.render(<App />, document.getElementById("root"));
