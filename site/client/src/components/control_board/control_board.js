const ControlBoard = () => {
	// this contains a series of buttons that allows the developer
	// to test certain features of the program
	return (
		<div className="ControlBoard">
			<h1>Control Board</h1>
		</div>
	);
};

export default ControlBoard;
